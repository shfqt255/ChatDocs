name: chatdocsflutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^11.0.2
  http: ^1.6.0
  provider: ^6.1.5+1
  supabase_flutter: ^2.15.3
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/icon.jpg

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon.jpg"